<configuration>
    # Console appender
<!--    <appender name="FILE" class="ch.qos.logback.core.FileAppender">-->
<!--        <file>log/log-odyssey.log</file>-->
    <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
<!--        <layout class="ch.qos.logback.classic.PatternLayout">-->
<!--            # Pattern of log message for console appender-->
<!--            <Pattern>{"time":"%d{yyyy-MM-dd-HH:mm:ss:SSS}" , "thread":"%t , "level":"%-5p", "source":"%logger{36} , "message":"%m"}%n</Pattern>-->
<!--        </layout>-->
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"application":"log-odyssey"}</customFields>
        </encoder>
    </appender>

    # File appender
    <appender name="fout" class="ch.qos.logback.core.FileAppender">
        <file>petshop.log</file>
        <append>false</append>
        <encoder>
            # Pattern of log message for file appender
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5p %m%n</pattern>
        </encoder>
    </appender>

    # Override log level for specified package
    <logger name="com.baeldung.log4j" level="TRACE"/>

    <root level="INFO">
        <appender-ref ref="stdout" />
        <appender-ref ref="fout" />
    </root>
</configuration>


